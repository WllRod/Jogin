@model Jogin.Models.UsernameModel;


    @using(Html.BeginForm("Index", "Home", FormMethod.Post))
    {
        <div style="width:100vw;height:100vh;display:flex;align-items:center;justify-content:center;background:black;color:white;flex-direction:column;">

            <span>Digite o seu nome</span>
            @Html.TextBoxFor(modelItem => modelItem.Username)
            <button>Salvar</button>
        </div>

    }

